# TODO

- [ ] Inspect existing frontend “Sort prices” implementation (discovery page).
- [ ] Inspect/confirm backend API patterns for listing data from D1.
- [ ] Add backend endpoint: list hotels from `hotelsdetail` with `ORDER BY price` based on `sort` param.
- [ ] Register backend route in `server/app.js`.
- [ ] Update frontend `src/components/Content/discovery.jsx` to fetch hotels list and apply selected sorting (and re-fetch on change).
- [ ] Ensure price values are numeric and consistent between DB and UI.
- [ ] Test locally: select “High” and “Lowest” and verify ordering.

